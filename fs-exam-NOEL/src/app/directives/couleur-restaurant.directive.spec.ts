import { CouleurRestaurantDirective } from './couleur-restaurant.directive';

describe('CouleurRestaurantDirective', () => {
  it('should create an instance', () => {
    const directive = new CouleurRestaurantDirective();
    expect(directive).toBeTruthy();
  });
});
